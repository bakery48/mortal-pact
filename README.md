# Mortal Pact

デッキ構築型ローグライト（ダークファンタジー）。Godot 4.x / ピクセルアート / ソロ開発。
詳細は企画設計書（`game_design.md`）を参照。

> 「育てた魔物はいつか老いる。だからこそ、命をつなげ。」

---

## 実装状況

### ✅ フェーズ1：コア戦闘（実装済み）
- [x] 戦闘画面の実装
- [x] カード（魔物）の召喚・コマンド選択
- [x] エネルギーシステム（毎ターン3回復・コスト1〜3）
- [x] 敵のHP・行動予告（intent）・攻撃
- [x] 山札 / 手札 / 捨札のデッキ操作（使用カードは捨札へ、山札が尽きたら再シャッフル）
- [x] 勝敗判定（敵HP0で勝利 / プレイヤーHP0で敗北）

### ✅ フェーズ2：ライフサイクル（実装済み）
- [x] 魔物の成長段階（🥚幼体 → 🌱若体 → ⚔️成体 → 🍂老体 → 💀消滅）
- [x] EXP蓄積と段階移行（コマンド使用ごとに+1 EXP、閾値で段階上昇）
- [x] 段階に応じた能力倍率（ピークの成体が最強、前後で弱体化）
- [x] 老体の弱体化（コスト+1のみ／能力倍率は若体相当）／幼体はコスト安め
- [x] 消滅でデッキから除外（捨札にも戻さない）
- [x] 成長・消滅時のイベント通知、カードに段階ラベルとEXPバーを表示

### ✅ フェーズ3：合体（血統強化）（実装済み）
- [x] 合体は**休憩/ショップノード**で行う。**血統（残る側）と相手（消える側）**を選ぶ
- [x] 名前・属性・基礎ステータス・成長速度・レアリティは**血統を継承**
- [x] **+値**で強化：`新しい+ = 血統の+ ＋ 相手の+ ＋ 1`（例：フェンリル×ピクシー＝フェンリル+1 ／ サラマンダー+2×ゴーレム+1＝サラマンダー+4）
  - +が**奇数に達するごとにATK+1**、**偶数に達するごとにDEF+1**（実効値に `ATK+⌈N/2⌉`・`DEF+⌊N/2⌋` を加算）
- [x] スキル＝固有2（血統の属性別基礎キット）＋ 両親から手動選択で継承
  - 継承できる数：親のスキル合計 4→1 / 6→2 / 8→3 / 10→4、1世代で+1まで、1体最大6（デッキ圧縮目的で0も可）
- [x] 両親はデッキから消滅し、合体後は「生まれたて」の幼体に戻って再育成

### ✅ フェーズ4：ラン構造（実装済み）
- [x] 分岐マップ（行ごとに複数ノード・ルート選択／バトル→休憩・ショップ→エリート→ボス）
- [x] 報酬（勝利後に新しい魔物カードを3択から獲得）
- [x] 休憩（HP回復）／ショップ（所持金で魔物を購入）
- [x] ゲームオーバー・フロアクリア処理
- [x] ラン状態のシングルトン化（デッキ・HP・所持金・進行をバトル間で持続）
- [x] フロアを進むごとに敵が強化される周回構造
- [x] デッキはカード実体を共有 → 成長・老化・子孫がラン全体で引き継がれる

### ✅ フェーズ5：コンテンツ拡充（実装済み）
- [x] 魔物の種類（カタログ46種、報酬・ショップに登場）
- [x] コマンド効果12種（ダメージ/与ダメ強化/2倍/回復/ガード/貫通/弱体化/エネルギー/毒/炎上/凍結/再生）でロール分化
- [x] プレイヤーのブロック（防御）処理
- [x] 状態異常システム（毒・炎上・凍結・再生／敵味方双方に付与可能）
- [x] 属性相性（6属性＋無。水>炎>風>土>水の4すくみ＋光⇔闇の相互弱点。有利×1.5/不利×0.75）
- [x] 敵の多様化（行動パターン：攻撃/防御/強化/回復、敵ブロック処理）
- [x] ボスの実装（複数行動パターンを持つ強敵3種）
- [x] BGM・SE（効果音は手続き生成で外部アセット無しでも再生）

これで設計書の MVP（フェーズ1〜5）が一通り実装済みです。

### ✅ 追加：セーブ・ロード（実装済み）
- [x] ラン状態（デッキの全カード状態・HP・所持金・フロア・進行・マップ）を JSON で保存
- [x] 保存先は `user://savegame.json`（設計書のユーザーディレクトリ＋JSON方針に準拠）
- [x] 起動時にセーブがあれば自動で続きから再開
- [x] マップ（ノード間の安全地点）に戻るたびに自動セーブ
- [x] マップの「最初からやり直す」で進行を消去して新規ラン

### ✅ 追加：メタ進行（ランをまたぐ引き継ぎ）（実装済み）
- [x] **継承（命をつなぐ）**：HP0でラン終了時、デッキの魔物から最大6体を選んで次ランの初期デッキに引き継ぐ（`scenes/ui/legacy.tscn`）
  - 引き継いだ魔物は**幼体にリセット**され、再び育て直す（スキル・属性・基礎ステータス・成長速度・レアリティは保持）
  - 6体未満を選んだ場合は不足分をデフォルト初期種で補充
  - 引き継ぎは `user://carryover.json` に保存し、次ラン開始時に1度だけ消費
- [x] **合体は休憩/ショップノードで**：血統と相手を選び、+値で強化（戦闘中の合体は廃止）
- [x] **図鑑**：倒した敵・仲間にした魔物を `user://unlocks.json` に永続記録。タイトルの「図鑑」から発見状況を閲覧（未発見は「？？？」）
  - 図鑑はゲームバランスには影響しない（報酬プールは全種のまま）
- [x] **チュートリアル**：初回起動時に自動表示する全8ページの遊び方ガイド（`scenes/ui/tutorial.tscn`）。スキップ可能、タイトルの「遊び方」からいつでも再表示。既読は `user://tutorial.done` に記録

---

## 動かし方

1. [Godot 4.3 以降](https://godotengine.org/) を用意する
2. Godot のプロジェクトマネージャから本リポジトリの `project.godot` を読み込む
3. ▶ 実行（メインシーンは `res://scenes/ui/title.tscn`。ラン状態は autoload `Run` が管理）

### ランの流れ
- タイトル画面から「はじめから」/「つづきから」で開始
- マップは**分岐型**（行ごとに複数ノード）。現在地から線で繋がった次の行のノードを選んで進む
- ノードはバトル（⚔雑魚 / ★エリート / 👑ボス）と休憩・ショップ（🏕）
- バトルに勝つと報酬で新しい魔物を獲得し、所持金を得る → マップへ戻る
- 休憩/ショップではHP回復か魔物の購入ができる
- ボスを倒すとフロアクリア → 次のフロアへ（敵が強化、デッキは継続）
- HPが0になるとゲームオーバー

### 戦闘の遊び方
- **デッキは個々の「スキルカード」で構成**（1モンスターが2スキルなら2枚としてデッキに入る）
- 山札から手札5枚を引く。どのモンスターのどのスキルを引くかは運次第＝**手札次第で誰で戦うか決まる**
- カードのコマンドボタンを押すとエネルギーを消費して発動
- **同じモンスターのスキルはどれを使ってもそのモンスターが成長**（EXP・段階・ATK/DEF・属性はモンスター単位で共有）
- モンスターが消滅すると、そのモンスターのスキルカードはすべてデッキから除外される
- 使ったスキルカードは捨札へ。エネルギーが続く限り行動可能
- 敵は**複数出現することがある**（最大3体）。**スキル選択→カーソルへ矢印が伸びる→対象をクリック**で命中（右クリック/Escで取消）。単体敵や非ターゲット技は即発動。全滅で勝利
- 演出：ダメージ/回復/毒の数字ポップ、敵のヒット点滅、被弾時の画面フラッシュ＆シェイク
- 「ターン終了」で敵が予告どおりに攻撃 → 次のプレイヤーターンへ
- **ATK/DEFがコマンドに反映**：ダメージ系は威力＋`ATK×係数`、ガード系はブロック＋`DEF×係数`
  - 係数は技ごとに設定可能（`CommandData.stat_scale`）。未設定なら自動でコスト依存（コスト1=0.4 / 2=0.7 / 3=1.0、重い大技ほどステータス依存大）
  - さらに成長段階の倍率・属性相性も乗る

### 合体（血統強化）
- カードを使い込んで **⚔️成体**（または 🍂老体）にすると、休憩/ショップノードで合体の親に選べる
- 「合体する」ボタン →**血統（残る側）**を1体、**相手（消える側）**を1体選ぶ
- 結果は血統の名前を引き継ぎ、`+値 = 血統の+ ＋ 相手の+ ＋ 1` で強化（+値は ATK→DEF→INT の3サイクルで分配）
- 属性は抽選：**血統60% / 相手30% / その他10%**（相性計算にのみ影響）
- スキルは固有2（血統の種族スキル先頭2枚）＋両親から手動継承（同名重複も可）。6枠埋まると種族固有の奥義が自動解放（継承不可）。合体後は🥚幼体に戻って再び育つ
- 老体も親に使えるので、消滅する前に「命をつなぐ」立ち回りができる

---

## ディレクトリ構成

```
res://
├── project.godot           # メインシーン=title.tscn、autoload に Run / Audio を登録
├── scenes/
│   ├── battle/
│   │   ├── battle.tscn / battle_manager.gd   # 戦闘シーン・進行管理
│   │   ├── card.tscn   / card.gd             # カードUI
│   │   └── enemy.tscn  / enemy.gd            # 敵UI
│   ├── map/
│   │   └── map.tscn    / map.gd              # ランマップ（分岐グラフ）
│   └── ui/
│       ├── reward.tscn / reward.gd           # 戦闘報酬（カード獲得）
│       ├── shop.tscn   / shop.gd             # 休憩・ショップ
│       ├── result.tscn / result.gd           # クリア / ゲームオーバー
│       ├── settings.tscn / settings.gd       # 設定（音量・フルスクリーン）
│       └── title.tscn  / title.gd            # タイトル（メインシーン）
├── scripts/
│   ├── command_data.gd     # コマンド（技）リソース定義
│   ├── monster_data.gd     # 魔物カードリソース＋ライフサイクル
│   ├── deck_manager.gd     # 山札/手札/捨札の操作
│   ├── monster_factory.gd  # 初期デッキ・魔物カタログ(46種)・合体ロジック
│   ├── enemy_database.gd   # 敵・ボスのカタログ（行動パターン付き）
│   ├── run_state.gd        # ラン全体の状態管理（autoload "Run"）
│   └── audio_manager.gd    # BGM/SE 管理（autoload "Audio"、SEは手続き生成）
├── resources/
│   └── monsters/           # 魔物データ（.tres カスタムリソース）
└── assets/
    ├── sprites/            # （ドット絵を配置予定）
    └── audio/              # bgm_battle.ogg / bgm_map.ogg を置くとBGM再生
```

### サウンド・設定
- 効果音(SE)は `audio_manager.gd` が起動時に波形を生成するため、アセット無しでも鳴る
- BGMは `assets/audio/bgm_battle.ogg` `assets/audio/bgm_map.ogg` を置けば自動再生（無ければ無音）
- マップの「設定」から音量（マスター/BGM/SE）とフルスクリーンを調整でき、`user://settings.json` に保存される

### セーブデータ
- `run_state.gd` が `user://savegame.json` にラン状態を JSON 保存・復元する
- `MonsterData` / `CommandData` の `to_dict()` / `from_dict()` でカードを直列化（成長段階・EXP・属性・コマンドまで保存）
- マップに戻るたびに自動セーブ。戦闘途中で終了した場合は、そのノードの開始時点から再開する

### データ管理
- 魔物・コマンドは `Resource` を継承したカスタムリソース（`MonsterData` / `CommandData`）
- 初期デッキは `resources/monsters/*.tres` から読み込み、見つからない場合は `MonsterFactory` がコードから生成
- 各カードはドロー時に複製され、独立したインスタンスになる。`exp` と `stage` を各カードが保持し、個別に成長・老化する

### バランス調整
- **全体デフレ係数 `MonsterData.POWER_SCALE`（現在0.6）** … ダメージ/HP/ATK/DEF/回復/毒量などの能力値を一律に縮める。コスト・エネルギー・状態異常の継続ターン・EXP閾値には掛からない＝バランス比率は維持。図鑑(`game_design.html`)の数値は素の基礎値で、実際のゲーム内はこの係数ぶん小さく表示される。

### ライフサイクルの調整
段階の閾値・能力倍率・コスト補正は `scripts/monster_data.gd` の定数
（`STAGE_THRESHOLDS` / `STAGE_MULT` / `STAGE_COST_DELTA`）で一括調整できる。
初期デッキは「生まれたて」の幼体から始まり、使い込むほど成長してピーク（成体）に達し、
さらに使うとやがて老体・消滅へ向かう。成長は速く、老化は長いランをかけて進む設定。
